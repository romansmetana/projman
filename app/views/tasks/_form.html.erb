<%= simple_form_for task, html: { id: "#{dom_id(task)}_form" }  do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.association :tags %>
  <%= f.association :project %>
  <%= f.submit %>
<% end %>
