<div class="mb-3 text-center">
  <%= link_to "Add task", new_task_path, data: {turbo_frame: "#{dom_id(Task.new)}_form"},
  class: 'btn bg-warning bg-opacity-25' %>
  <%= turbo_frame_tag "#{dom_id(Task.new)}_form" %>
</div>
<div id="tasks">
  <%= render @tasks %>
</div>
