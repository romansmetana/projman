<ul id="<%= "#{dom_id(tag)}_item" %>">
  <%= turbo_frame_tag dom_id(tag) do %>
    <div class="d-flex justify-content-between">
      <%= link_to tag.title, edit_tag_path(tag) %>
      <%= button_to t("helpers.submit.tag.delete"), tag_path(tag), method: :delete %>
    </div>
  <% end %>
</ul>
