<div id="<%= "#{dom_id(tag)}_item" %>">
  <%= turbo_frame_tag dom_id(tag) do %>
    <div class="p-1 me-1">
      <small class="px-2 py-1 border rounded-pill"> <%= tag.title %></small>
    </div>
  <% end %>
</div>
